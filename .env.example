# Production — used by npm run build (via .env.production)
VITE_SUBMISSION_API_URL=https://s2pod1tkk6.execute-api.us-east-1.amazonaws.com/Default/price-submission

# Development — npm run dev uses .env.development (staging/test keys by default)
# VITE_SUBMISSION_API_URL=https://s2pod1tkk6.execute-api.us-east-1.amazonaws.com/Default/price-submission-staging
